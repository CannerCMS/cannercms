
extends ./layout.pug

block head
  script.
    window.username = "#{username}";
    window.thumbnail = "#{thumbnail}";
block main
  div(id='root')
  script(src= `${staticsUrl}/vendors.js`)
  script(src= `${staticsUrl}/index.js`)
block footerScript
