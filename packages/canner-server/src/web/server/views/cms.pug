
extends ./layout.pug

block head
  script.
    window.username = "#{username}";
    window.thumbnail = "#{thumbnail}";
block main
  div(id='root')
  script(src= `${staticsPath}/vendors.js`)
  script(src= `${staticsPath}/index.js`)
block footerScript
