import ActionManager from '../../src/action/index';

describe('actionManager', () => {
  let actionManager;
  beforeEach(() => {
    actionManager = new ActionManager();
  });

  it('should addAction', () => {

  });

  it('should getAction', () => {
    // actionManger.getAction(key, id)
  })
});